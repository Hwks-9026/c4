fn main() -> i32 {
	let x | i32 <- 4;
	x <- add(x,4);
	@x {
		1 -> ret 2;
		~ -> {
			
		};
	}	

	ret x;
}

fn add(a | num, b | num) -> num {
	ret a + b;
} 

fn sad(a | bob) -> bool {
	ret bob.sad;
}

struct bob {
	sad | bool
}
